version: "3.7"
services:

  spanner:
    container_name: jsql-spanner
    # issue on latest
    # NoSuchMethodError registerLongUpDownCounter
    # NoClassDefFoundError SubchannelMetrics
    image: gcr.io/cloud-spanner-emulator/emulator:1.5.50

    ports:
      - "9010:9010"
    privileged: true  # required on local
    networks:
      - jsql-network

networks:
  jsql-network: